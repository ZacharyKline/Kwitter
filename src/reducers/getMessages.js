const initialState = {
    messages: []
};
// added GET_Messages file - Tamekia

export default (state = initialState, action) => {
    switch (action.type) {
        case GET_Messages_Success:
            
        default:
            return state;
    }
}